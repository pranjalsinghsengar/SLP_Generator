import React from "react";
import styled from "styled-components";

const CardContainer = styled.div``;
const Card = () => {
  return <CardContainer className='shadow-xl'>Card</CardContainer>;
};

export default Card;
